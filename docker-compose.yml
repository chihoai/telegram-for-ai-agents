services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: tgchats
    ports:
      - "5432:5432"
    volumes:
      - tgchats-postgres:/var/lib/postgresql/data

volumes:
  tgchats-postgres:

