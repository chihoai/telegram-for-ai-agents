# Cloud MCP

Required scopes:

- `telegram.read`
- `telegram.members.invite`

Tools:

- `dialogs_list`
- `chat_read`
- `members_invite_preview`
- `members_invite_approved`

Use `members_invite_preview` for every run. Approval mode controls whether the user must confirm, not whether a preview is created.
