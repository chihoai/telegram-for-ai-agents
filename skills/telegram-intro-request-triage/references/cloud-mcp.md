# Cloud MCP

Required scopes:

- `telegram.read`
- `crm.write`
- `telegram.message.preview` when drafting previews

Useful tools:

- `search.messages`
- `chat.read`
- `tasks.add`
- `tags.set`
- `nudge.generate`
- `outbox.preview`
