# Cloud MCP

Required scopes:

- `telegram.read`
- `crm.write`
- `telegram.message.preview` for update previews

Useful tools:

- `search.messages`
- `chat.read`
- `summary.refresh`
- `tasks.add`
- `nudge.generate`
- `outbox.preview`
